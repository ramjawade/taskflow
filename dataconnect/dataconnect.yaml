specVersion: "v1"
serviceId: "taskflow"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "taskflow-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
